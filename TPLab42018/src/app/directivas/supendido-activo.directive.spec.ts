import { SupendidoActivoDirective } from './supendido-activo.directive';

describe('SupendidoActivoDirective', () => {
  it('should create an instance', () => {
    const directive = new SupendidoActivoDirective();
    expect(directive).toBeTruthy();
  });
});
