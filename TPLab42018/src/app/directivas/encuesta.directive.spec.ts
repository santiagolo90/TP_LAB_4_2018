import { EncuestaDirective } from './encuesta.directive';

describe('EncuestaDirective', () => {
  it('should create an instance', () => {
    const directive = new EncuestaDirective();
    expect(directive).toBeTruthy();
  });
});
