import { ViajeDirective } from './viaje.directive';

describe('ViajeDirective', () => {
  it('should create an instance', () => {
    const directive = new ViajeDirective();
    expect(directive).toBeTruthy();
  });
});
